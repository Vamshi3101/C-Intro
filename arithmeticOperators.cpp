#include<iostream>
#include<math.h>
using namespace std;
int main(){
    int a,s;
    cin>>a>>s;
    cout<<a+s<<" "<<a-s<<" "<<a*s<<" "<<double(a)/s<<" "<<a/s<<" "<<a%s<<" "<<pow(float(a),float(s));
    return 0;
}